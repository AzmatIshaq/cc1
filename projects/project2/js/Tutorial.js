// Tutorial Class

class Tutorial {
  constructor(startColT, startRowT) {
    this.width = 99;
    this.height = 19;
    this.outlineWidth = 2;
    this.outlineHeight = 2;
    this.x = 0;
    this.y = 0;
    this.outlineX = 1;
    this.outlineY = 1;
    this.name = "tutorial";
    this.currentCol = startColT;
    this.currentRow = startRowT;
  }

  // move() {}

  display() {

  // Display tutorial dialogue depending on checkpoint name
//
//   if (grid[this.currentCol][this.currentRow].name === ``) {
    // push();
    // fill(255);
    // textSize(16);
    // textAlign(CENTER, CENTER);
    // text(`Fog of War!`, width / 2, height / 1.07);
    // text(`Find another Fog of War item to disable it!`, width / 2, height / 1.04);
    // pop();
//   }
 }
}
